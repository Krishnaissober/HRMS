import { NextRequest } from "next/server"; import { errorResponse } from "@/lib/errors"; import { requestId, successResponse } from "@/lib/request"; import { parseQuery } from "@/lib/validate"; import { getAuthenticatedContext } from "@/lib/tenant"; import { requirePermission } from "@/lib/rbac"; import { LEAVE_PERMISSIONS } from "@/modules/leave/constants"; import { leaveCalendarSchema } from "@/modules/leave/schemas"; import { leaveCalendar } from "@/modules/leave/service";
export async function GET(request:NextRequest){const id=requestId(request);try{const context=await getAuthenticatedContext(request);await requirePermission(context.session.user.id,context.organizationId,LEAVE_PERMISSIONS.read);return successResponse(await leaveCalendar(context.organizationId,parseQuery(leaveCalendarSchema,request.nextUrl.searchParams),context.session.user.email),id);}catch(error){return errorResponse(error,id);}}
