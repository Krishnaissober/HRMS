"use client";

import { useCallback, useEffect, useMemo, useState, type ReactNode } from "react";
import { BarChart3, CalendarDays, Download, Filter, RefreshCw, TrendingUp } from "lucide-react";
import { cn } from "@/lib/utils";

type Domain = "recruitment" | "workforce" | "attendance" | "leave" | "hr" | "payroll" | "audit";
type ApiState = { loading: boolean; error: string; data: Record<string, unknown> | null };
const navigation: Array<{ label: string; domains: Domain[] }> = [
  { label: "Recruitment", domains: ["recruitment"] }, { label: "Workforce", domains: ["workforce"] },
  { label: "Attendance", domains: ["attendance"] }, { label: "Leave", domains: ["leave"] }, { label: "HR KPIs", domains: ["hr", "payroll", "audit"] },
];
const labels: Record<Domain, string> = { recruitment: "Recruitment", workforce: "Workforce", attendance: "Attendance", leave: "Leave", hr: "Onboarding and HR operations", payroll: "Payroll and expenses", audit: "Audit" };
function title(value: string) { return value.replace(/([a-z])([A-Z])/g, "$1 $2").replaceAll("_", " ").replace(/^./, (letter) => letter.toUpperCase()); }

function Value({ name, value }: { name: string; value: unknown }): ReactNode {
  if (value == null || typeof value !== "object") return <div className="flex items-center justify-between gap-4 rounded-2xl border border-slate-200/80 bg-white/80 px-4 py-3 dark:border-slate-800 dark:bg-slate-950/30"><span className="text-sm font-semibold text-slate-500 dark:text-slate-400">{title(name)}</span><strong className="text-right text-lg font-black tracking-tight text-slate-950 dark:text-white">{value == null || value === "" ? "—" : String(value)}</strong></div>;
  if (Array.isArray(value)) {
    if (value.length === 0) return <p className="rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-6 text-sm font-medium text-slate-500 dark:border-slate-700 dark:bg-slate-950/20 dark:text-slate-400">No records in this period.</p>;
    const keys = [...new Set(value.flatMap((row) => row && typeof row === "object" ? Object.keys(row as object) : ["value"]))];
    return <div className="overflow-x-auto rounded-2xl border border-slate-200/80 dark:border-slate-800"><table className="min-w-full divide-y divide-slate-200 text-left dark:divide-slate-800"><thead className="bg-slate-50/80 dark:bg-slate-950/50"><tr>{keys.map((key) => <th key={key} className="whitespace-nowrap px-4 py-3 text-[11px] font-extrabold uppercase tracking-wider text-indigo-600 dark:text-indigo-300">{title(key)}</th>)}</tr></thead><tbody className="divide-y divide-slate-100 bg-white dark:divide-slate-800/70 dark:bg-transparent">{value.map((row, index) => <tr key={index} className="transition-colors hover:bg-indigo-50/40 dark:hover:bg-indigo-950/20">{keys.map((key) => <td key={key} className="whitespace-nowrap px-4 py-3 text-sm font-medium text-slate-700 dark:text-slate-300">{row && typeof row === "object" ? String((row as Record<string, unknown>)[key] ?? "—") : String(row)}</td>)}</tr>)}</tbody></table></div>;
  }
  const object = value as Record<string, unknown>;
  if (name === "drilldowns") return <div className="flex flex-wrap gap-3">{Object.entries(object).map(([key, path]) => <a key={key} href={String(path)} className="inline-flex items-center gap-2 rounded-xl border border-indigo-200 bg-indigo-50 px-4 py-2.5 text-sm font-bold text-indigo-700 transition hover:-translate-y-0.5 hover:bg-indigo-100 dark:border-indigo-900 dark:bg-indigo-950/40 dark:text-indigo-300">View {title(key)} records <TrendingUp className="h-4 w-4" /></a>)}</div>;
  return <div className="space-y-3 rounded-2xl border border-slate-200/80 bg-slate-50/70 p-4 dark:border-slate-800 dark:bg-slate-950/20"><h3 className="text-sm font-extrabold uppercase tracking-wider text-slate-500 dark:text-slate-400">{title(name)}</h3><div className="grid gap-3 sm:grid-cols-2">{Object.entries(object).map(([key, child]) => <Value key={key} name={key} value={child} />)}</div></div>;
}

export default function ReportsPage() {
  const [active, setActive] = useState(0); const [from, setFrom] = useState(""); const [to, setTo] = useState(""); const [department, setDepartment] = useState(""); const [states, setStates] = useState<Partial<Record<Domain, ApiState>>>({}); const domains = useMemo(() => navigation[active].domains, [active]);
  const load = useCallback(async (selected: Domain[]) => { setStates((current) => ({ ...current, ...Object.fromEntries(selected.map((domain) => [domain, { loading: true, error: "", data: null }])) })); const query = new URLSearchParams(); if (from) query.set("from", from); if (to) query.set("to", to); if (department) query.set("department", department); await Promise.all(selected.map(async (domain) => { try { const response = await fetch(`/api/v1/analytics/${domain}?${query}`); const result = await response.json(); setStates((current) => ({ ...current, [domain]: response.ok ? { loading: false, error: "", data: result.data } : { loading: false, error: response.status === 403 ? "This report is outside your assigned permissions." : result.error?.message || "Could not load this report.", data: null } })); } catch { setStates((current) => ({ ...current, [domain]: { loading: false, error: "Could not reach the reporting service.", data: null } })); } })); }, [department, from, to]);
  useEffect(() => { void load(domains); }, [domains, load]);
  function exportUrl(domain: Domain) { const query = new URLSearchParams(); if (from) query.set("from", from); if (to) query.set("to", to); if (department) query.set("department", department); return `/api/v1/reports/${domain}/export?${query}`; }
  return <main className="page-shell space-y-6 pb-12">
    <section className="relative overflow-hidden rounded-3xl border border-indigo-500/20 bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900 p-6 text-white shadow-2xl md:p-8"><div className="pointer-events-none absolute -right-16 -top-20 h-64 w-64 rounded-full bg-indigo-500/20 blur-3xl" /><div className="pointer-events-none absolute bottom-0 right-1/3 h-40 w-40 rounded-full bg-purple-500/20 blur-3xl" /><div className="relative z-10 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div className="space-y-2"><p className="text-xs font-extrabold uppercase tracking-[0.2em] text-indigo-300">Reports &amp; analytics</p><h1 className="text-3xl font-black tracking-tight md:text-4xl">See the whole picture</h1><p className="max-w-2xl text-sm font-medium leading-6 text-indigo-100/75">Company-scoped metrics calculated from persisted HR records. Explore a domain, filter the period, and drill into the underlying records.</p></div><button type="button" onClick={() => void load(domains)} className="inline-flex shrink-0 items-center justify-center gap-2 rounded-2xl border border-white/20 bg-white/10 px-4 py-3 text-sm font-bold text-white shadow-lg backdrop-blur-md transition hover:-translate-y-0.5 hover:bg-white/20 active:scale-95"><RefreshCw className="h-4 w-4" /> Refresh report</button></div></section>
    <section className="rounded-3xl border border-slate-200/80 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900/80 md:p-6"><div className="mb-5 flex items-center gap-2"><BarChart3 className="h-5 w-5 text-indigo-500" /><p className="text-xs font-extrabold uppercase tracking-[0.18em] text-indigo-600 dark:text-indigo-300">Report workspace</p></div><nav className="flex flex-wrap gap-2" aria-label="Report areas">{navigation.map((item, index) => <button key={item.label} type="button" aria-current={active === index ? "page" : undefined} onClick={() => setActive(index)} className={cn("rounded-xl px-4 py-2.5 text-sm font-bold transition", active === index ? "bg-indigo-600 text-white shadow-lg shadow-indigo-500/20" : "bg-slate-100 text-slate-600 hover:-translate-y-0.5 hover:bg-indigo-50 hover:text-indigo-700 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-indigo-950/50 dark:hover:text-indigo-300")}>{item.label}</button>)}</nav><div className="mt-6 grid gap-4 rounded-2xl border border-slate-200/80 bg-slate-50/70 p-4 dark:border-slate-800 dark:bg-slate-950/30 md:grid-cols-[1fr_1fr_1.4fr_auto] md:items-end"><label className="space-y-2 text-xs font-extrabold uppercase tracking-wider text-slate-500 dark:text-slate-400">From<div className="relative"><CalendarDays className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-indigo-500" /><input type="date" value={from} onChange={(event) => setFrom(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 pl-9 text-sm font-semibold text-slate-800 outline-none transition focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/10 dark:border-slate-700 dark:bg-slate-900 dark:text-white" /></div></label><label className="space-y-2 text-xs font-extrabold uppercase tracking-wider text-slate-500 dark:text-slate-400">To<div className="relative"><CalendarDays className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-indigo-500" /><input type="date" value={to} onChange={(event) => setTo(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 pl-9 text-sm font-semibold text-slate-800 outline-none transition focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/10 dark:border-slate-700 dark:bg-slate-900 dark:text-white" /></div></label><label className="space-y-2 text-xs font-extrabold uppercase tracking-wider text-slate-500 dark:text-slate-400">Department<input value={department} maxLength={120} onChange={(event) => setDepartment(event.target.value)} placeholder="All departments" className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-semibold normal-case tracking-normal text-slate-800 outline-none transition placeholder:text-slate-400 focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/10 dark:border-slate-700 dark:bg-slate-900 dark:text-white" /></label><button type="button" onClick={() => void load(domains)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-4 py-3 text-sm font-bold text-white shadow-lg shadow-indigo-500/20 transition hover:-translate-y-0.5 hover:bg-indigo-700 active:scale-95"><Filter className="h-4 w-4" /> Apply</button></div></section>
    {domains.map((domain) => { const state = states[domain]; return <section key={domain} className="rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900/80 md:p-6"><div className="mb-5 flex flex-col justify-between gap-3 border-b border-slate-100 pb-5 dark:border-slate-800 sm:flex-row sm:items-center"><div><p className="text-xs font-extrabold uppercase tracking-[0.18em] text-indigo-600 dark:text-indigo-300">Analytics domain</p><h2 className="mt-1 text-2xl font-black tracking-tight text-slate-950 dark:text-white">{labels[domain]}</h2></div>{state?.data && <a href={exportUrl(domain)} className="inline-flex items-center gap-2 self-start rounded-xl border border-indigo-200 bg-indigo-50 px-4 py-2.5 text-sm font-bold text-indigo-700 transition hover:-translate-y-0.5 hover:bg-indigo-100 dark:border-indigo-900 dark:bg-indigo-950/40 dark:text-indigo-300"><Download className="h-4 w-4" /> Export CSV</a>}</div>{(!state || state.loading) && <p role="status" className="rounded-2xl border border-dashed border-indigo-200 bg-indigo-50/60 p-6 text-sm font-semibold text-indigo-700 dark:border-indigo-900 dark:bg-indigo-950/20 dark:text-indigo-300">Loading {labels[domain].toLowerCase()} analytics…</p>}{state?.error && <p role="alert" className="rounded-2xl border border-rose-200 bg-rose-50 p-4 text-sm font-semibold text-rose-700 dark:border-rose-900 dark:bg-rose-950/20 dark:text-rose-300">{state.error}</p>}{state?.data && <div className="space-y-4">{Object.entries(state.data).filter(([key]) => !["domain", "filters", "range"].includes(key)).map(([key, value]) => <Value key={key} name={key} value={value} />)}</div>}</section>; })}
  </main>;
}
