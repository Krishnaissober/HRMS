import { NextRequest } from "next/server"; import { errorResponse } from "@/lib/errors"; import { requestId, successResponse } from "@/lib/request"; import { parseBody, parseQuery } from "@/lib/validate"; import { getAuthenticatedContext } from "@/lib/tenant"; import { requirePermission } from "@/lib/rbac"; import { LEAVE_PERMISSIONS } from "@/modules/leave/constants"; import { leaveListSchema, leaveRequestSchema } from "@/modules/leave/schemas"; import { requestLeave, scopedLeaveRequests } from "@/modules/leave/service";
export async function GET(request:NextRequest){const id=requestId(request);try{const context=await getAuthenticatedContext(request);await requirePermission(context.session.user.id,context.organizationId,LEAVE_PERMISSIONS.read);return successResponse(await scopedLeaveRequests({organizationId:context.organizationId,actorEmail:context.session.user.email,query:parseQuery(leaveListSchema,request.nextUrl.searchParams)}),id);}catch(error){return errorResponse(error,id);}}
export async function POST(request:NextRequest){const id=requestId(request);try{const context=await getAuthenticatedContext(request);await requirePermission(context.session.user.id,context.organizationId,LEAVE_PERMISSIONS.request);const body=parseBody(leaveRequestSchema,await request.json());return successResponse(await requestLeave({organizationId:context.organizationId,actorUserId:context.session.user.id,userEmail:context.session.user.email,...body,requestId:id}),id,{status:201});}catch(error){return errorResponse(error,id);}}
