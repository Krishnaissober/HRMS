import { NextRequest } from "next/server"; import { errorResponse } from "@/lib/errors"; import { requestId, successResponse } from "@/lib/request"; import { getAuthenticatedContext } from "@/lib/tenant"; import { requirePermission } from "@/lib/rbac"; import { LEAVE_PERMISSIONS } from "@/modules/leave/constants"; import { selfLeaveDashboard } from "@/modules/leave/service";
export async function GET(request:NextRequest){const id=requestId(request);try{const context=await getAuthenticatedContext(request);await requirePermission(context.session.user.id,context.organizationId,LEAVE_PERMISSIONS.read);const year=request.nextUrl.searchParams.get("year");return successResponse(await selfLeaveDashboard({organizationId:context.organizationId,userEmail:context.session.user.email,year:year?Number(year):undefined}),id);}catch(error){return errorResponse(error,id);}}
