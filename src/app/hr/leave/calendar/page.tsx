"use client";
/* eslint-disable react-hooks/exhaustive-deps */
import { useEffect, useState } from "react";
type View="day"|"week"|"month"; type Request={id:string;startDate:string;endDate:string;status:string;employee:{employeeNo:string;firstName:string;lastName:string};leaveType:{name:string}};
export default function LeaveCalendarPage(){const[view,setView]=useState<View>("month");const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[requests,setRequests]=useState<Request[]>([]);const[range,setRange]=useState("");const[message,setMessage]=useState("Loading calendar…");async function load(nextView=view){const response=await fetch(`/api/v1/leave-calendar?view=${nextView}&date=${date}`);const result=await response.json();if(!response.ok)return setMessage(result.error?.message||"Could not load leave calendar");setRequests(result.data.requests);setRange(`${result.data.from} to ${result.data.to}`);setMessage("")}useEffect(()=>{void load()},[]);return <main className="page-shell"><section className="panel"><p className="eyebrow">Manager / HR</p><h1>Leave calendar</h1><div className="toolbar">{(["day","week","month"] as View[]).map(item=><button key={item} type="button" aria-pressed={view===item} onClick={()=>{setView(item);void load(item)}}>{item[0].toUpperCase()+item.slice(1)}</button>)}<input type="date" value={date} onChange={e=>setDate(e.target.value)}/><button type="button" onClick={()=>void load()}>Apply</button></div>{message&&<p role="status">{message}</p>}<h2>{view} · {range}</h2><div className="candidate-list" data-testid={`leave-${view}-calendar`}>{requests.map(request=><div className="candidate-row" key={request.id}><span>{request.employee.employeeNo} · {request.employee.firstName} {request.employee.lastName}</span><span>{request.leaveType.name}</span><span>{request.startDate.slice(0,10)} to {request.endDate.slice(0,10)}</span><span>{request.status}</span></div>)}</div></section></main>}
