import { NextRequest } from "next/server"; import { errorResponse } from "@/lib/errors"; import { requestId, successResponse } from "@/lib/request"; import { parseBody } from "@/lib/validate"; import { getAuthenticatedContext } from "@/lib/tenant"; import { requirePermission } from "@/lib/rbac"; import { LEAVE_PERMISSIONS } from "@/modules/leave/constants"; import { leaveDecisionSchema } from "@/modules/leave/schemas"; import { decideLeave } from "@/modules/leave/service";
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const requestIdentifier=requestId(request);try{const context=await getAuthenticatedContext(request);await requirePermission(context.session.user.id,context.organizationId,LEAVE_PERMISSIONS.approve);const body=parseBody(leaveDecisionSchema,await request.json());return successResponse(await decideLeave({organizationId:context.organizationId,actorUserId:context.session.user.id,actorEmail:context.session.user.email,id:(await params).id,...body,requestId:requestIdentifier}),requestIdentifier);}catch(error){return errorResponse(error,requestIdentifier);}}
