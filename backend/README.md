# HRMS backend

This folder is the first step of the backend separation. It currently exposes
a standalone health endpoint on port 4000. Existing Next.js API routes remain
in place until they are migrated one module at a time.
